[project]
name = "raidex-bot"
version = "1.0.0"
requires-python = "~=3.12.0"
dependencies = [
    "python-telegram-bot>=21,<23",
    "python-dotenv>=1,<2",
]

[tool.vercel]
entrypoint = "api/index.py"
